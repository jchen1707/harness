# The env contract, committed. Real values live in .env, which is gitignored and which the
# harness refuses to read as well as to write - a value read once is in the transcript.
DATABASE_URL=postgresql://localhost:5432/__PROJECT__
